name: Mise à jour complète (providers, domains, manifest)

on:
  push:
    paths:
      - 'sources.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Lancer le script de mise à jour
        run: node update.js
      - name: Commit et push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add providers/*.js domains.json manifest.json
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Mise à jour automatique (providers, domains, manifest)"
          git push
